import { StyleSheet } from "react-native";

const sessionListStyles = StyleSheet.create({

    mapContainer: {
        width: '75%',
        height: 200,
        marginBottom: 20,
        padding: 0,
        borderWidth: 2,
        borderColor: 'green',
        borderRadius: 30,
        elevation: 5,
        overflow: 'hidden'
    },
})

export default sessionListStyles;
